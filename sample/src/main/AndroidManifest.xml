<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ru.mobileup.rxplce.sample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name="ru.mobileup.rxplce.sample.LaunchActivity"
            android:launchMode="singleInstance"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <activity
            android:name="ru.mobileup.rxplce.sample.paging.PagingActivity"
            android:label="@string/label_paging"/>
        <activity
            android:name="ru.mobileup.rxplce.sample.loading.LoadingActivity"
            android:label="@string/label_loading"/>
        <activity
            android:name="ru.mobileup.rxplce.sample.refreshing.RefreshingActivity"
            android:label="@string/label_refreshing"/>

    </application>

</manifest>
